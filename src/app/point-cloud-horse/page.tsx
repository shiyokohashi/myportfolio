"use client";

import { HorseCorridorHero } from "@/components/HorseCorridorHero";

/** Isolated corridor test — same scene as the homepage hero. */
export default function PointCloudHorseTestPage() {
  return <HorseCorridorHero />;
}
