import { HomeNextIteration } from "./HomeNextIteration";

export const metadata = {
  title: "Home (next)",
  robots: { index: false, follow: false },
};

/** Isolated homepage sandbox — edit files in this folder only. */
export default function HomeNextPage() {
  return <HomeNextIteration />;
}
